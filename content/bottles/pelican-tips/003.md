Date: 2026-04-12 14:00
Tags: python

`pelicanconf.py` で設定したカスタム変数は、テンプレート内でそのまま参照できる。

```python
# pelicanconf.py
MY_CUSTOM_VAR = "Hello!"
```

```jinja2
{# テンプレート内 #}
<p>{{ MY_CUSTOM_VAR }}</p>
```

大文字のみの変数名がテンプレートに渡される仕様なので注意。
