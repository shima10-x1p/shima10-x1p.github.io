Date: 2026-04-12 11:30
Tags: pelican, jinja2

テーマのテンプレートで `article.summary` が空の場合にフォールバックするには Jinja2 の `default` フィルターが便利。

```jinja2
{{ article.summary | default(article.content, true) | striptags | truncate(160) }}
```

第2引数に `true` を渡すと、空文字列の場合もデフォルト値が使われる。
