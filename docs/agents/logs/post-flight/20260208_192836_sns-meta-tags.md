#### Postflight
* 変更内容: `index.html` と `article.html` に `extra_head` ブロックを追加し、Twitterカード（`summary`）とOGP向けmetaを設定。記事ページではサムネイルがある場合のみ画像metaを出力するようにした。
* 変更理由: SNS共有時にタイトル・概要・画像が適切に表示されるようにするため。要件に合わせてIndexはサイト情報のみ、記事ページは記事情報を出し分けるため。
* 影響範囲: 共有リンクのプレビュー表示に影響。URL構造や本文表示、Pelican設定値（SITEURL等）には変更なし。
* 変更ファイル一覧:
  * `themes/shima-island/templates/article.html`
  * `themes/shima-island/templates/index.html`
  * `docs/agent/logs/pre-flight/20260208_192727_sns-meta-tags.md`
  * `docs/agent/logs/post-flight/20260208_192836_sns-meta-tags.md`
* 実行したコマンドと結果:
  * `uv run pelican content -o output -s pelicanconf.py` -> 成功（1 article を処理）
* 確認方法:
  1. `uv run pelican content -o output -s pelicanconf.py` を実行する。
  2. 生成された `output/` の記事ページとトップページのHTMLを開き、`<head>` 内に `twitter:*` と `og:*` metaがあることを確認する。
  3. 記事にサムネイルがある場合のみ `twitter:image` / `og:image` が出ることを確認する。
* 残タスク: 必要に応じて `publishconf.py` での本番URL設定下で実共有プレビュー（X Card Validator等）を確認する。
